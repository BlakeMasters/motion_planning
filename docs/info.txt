2) Build the image

From that directory:

docker build -t waymo-motion:1.6.7 .

3) Run it with your folder mounted
On Windows PowerShell:
docker run --rm -it -p 8888:8888 -v ${PWD}:/work waymo-motion:1.6.7

On Git Bash (MINGW64):
docker run --rm -it -p 8888:8888 -v "$(pwd)":/work waymo-motion:1.6.7


Then open in your browser:
http://localhost:8888

4) Point the notebook at your shard

If you downloaded a file like:
training_20s.tfrecord-00000-of-01000

Put it in your project folder, then in the notebook:

DATASET_FOLDER = "/work"
VALIDATION_FILES = "/work/training_20s.tfrecord-00000-of-01000"


And your existing parsing line:

scenario = scenario_pb2.Scenario.FromString(bytes_example)


should work.

Quick sanity check (inside the notebook or a terminal in Jupyter)
import tensorflow as tf
from waymo_open_dataset.protos import scenario_pb2
print(tf.__version__)
print("scenario_pb2 OK")

One optional improvement: GPU support

If you have an NVIDIA GPU and want TF GPU, we can swap base images and install CUDA-enabled TF; but for “inspect a sample + visualize”, CPU is totally fine.

If you tell me your OS shell (PowerShell vs Git Bash) and whether you want GPU, I’ll tailor the exact docker run command + a GPU-enabled Dockerfile if needed.
    


You installed a subset of Waymo’s pinned deps (absl/dask/einsum/google-auth/immutabledict) and then installed Waymo with --no-deps.

That’s fine if you end up not missing anything at import-time, but Waymo’s wheel normally pulls more (often TF, protobuf/grpc, etc.). If you see ModuleNotFoundError later, this is why.